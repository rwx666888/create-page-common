${{if _modelType == 'new'}}
/* ${{apiConfig.fileDesc}} */
import { ${{apiConfig.type}} } from '${{_httpFile}}'
${{/if}}
// ${{apiConfig.desc}}
export const ${{apiConfig.name}} = (data) => ${{apiConfig.type}}(`${{_uri}}`, data)
