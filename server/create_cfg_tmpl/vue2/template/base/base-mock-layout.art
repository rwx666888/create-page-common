const demo = require('./data/example-data.js')
/**
 * 获取客户端传递过来的数值
 * config.param('name')，自动尝试从 config.params，config.body，config.query 中查询 name的valeu
 * post 类型：config.body
 * get 类型：config.query
 * 使用建议：
 * 1、每个业务模块一个路由文件；
 * 2、大量且复杂的 data-template 建议放在 ./data 目录下，例如 const demo = require('./data/example-data.js')
 * 3、不建议使用 all 方法；
 */

module.exports = []
